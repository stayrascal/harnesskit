import { describe, expect, test } from "bun:test";

describe("App", () => {
	test("renders without crashing", () => {
		// Basic smoke test — full rendering tests need jsdom/happy-dom
		expect(true).toBe(true);
	});
});
